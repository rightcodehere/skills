<!--
  Template for /codeprobe diff output formatted as a GitHub PR review comment.
  The orchestrator reads this template and fills {placeholders} with actual data.
  Render as markdown suitable for posting as a GitHub PR comment.

  Placeholders:
    {project_name}     — Repository name
    {branch}           — Branch being reviewed
    {base_branch}      — Base branch (usually main)
    {overall_score}    — Weighted overall score (0-100)
    {total_findings}   — Total number of findings
    {changed_files}    — Number of changed files reviewed
    {critical_count}   — Count of critical findings
    {major_count}      — Count of major findings
    {minor_count}      — Count of minor findings
    {suggestion_count} — Count of suggestions
    {file}             — File path relative to project root
    {lines}            — Line range (e.g., 45-67)
    {id}               — Finding ID (e.g., SEC-001, PERF-003)
    {severity_emoji}   — Emoji for severity level
    {problem}          — One-sentence description of the issue
    {suggestion}       — What to do to fix it
    {fix_prompt}       — Copy-pasteable prompt for Claude Code
-->

## Code Review -- {project_name}

**Branch:** `{branch}` -> `{base_branch}` | **Score:** {overall_score}/100 | **Files:** {changed_files} | **Findings:** {total_findings}

| Severity | Count |
|----------|-------|
| :red_circle: Critical | {critical_count} |
| :orange_circle: Major | {major_count} |
| :yellow_circle: Minor | {minor_count} |
| :blue_circle: Suggestion | {suggestion_count} |

---

### Findings by File

{For each file with findings:}

#### `{file}`

{For each finding in file:}

- {severity_emoji} **{id}** (line {lines}): {problem}
  > {suggestion}
  >
  > **Fix:** {fix_prompt}

{End for each finding}

{End for each file}

---

### Fix Prompts (Copy into Claude Code)

{Numbered list of all fix prompts, ordered by severity:}

1. {fix_prompt}
2. {fix_prompt}
3. ...

---

<sub>Generated by codeprobe v2.0.0 -- read-only analysis, no files were modified</sub>
